<%= form_for(@parent_of) do |f| %>
  <% if @parent_of.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parent_of.errors.count, "error") %> prohibited this parent_of from being saved:</h2>

      <ul>
      <% @parent_of.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :parent_id %><br>
    <%= f.text_field :parent_id %>
  </div>
  <div class="field">
    <%= f.label :child_id %><br>
    <%= f.text_field :child_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
